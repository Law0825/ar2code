
# AR2Code 围栏模块保护分析报告

## 一、现状分析

### AET 围栏模块保护完整功能
AET的围栏模块保护是一个完整的三层保护机制，包含：

1. **保护点一：设计阶段**
   - 通过 `skill-req-design` 生成包含围栏配置的SDD
   - 明确标记 ✅允许修改、❌禁止修改、🔵条件修改的文件范围
   - 基于模块依赖关系和架构原则自动生成

2. **保护点二：实现阶段**
   - `agent-implement` 在编码前检查围栏配置
   - 遇到禁止修改时自动停止并请求用户授权
   - 严格遵守任务中的围栏约束

3. **保护点三：PR阶段**
   - `skill-checking-implementation` 最终验证
   - 确保实际变更与围栏配置一致
   - 提供变更边界核验

### AR2Code 当前状态
AR2Code目前有：
1. ✅ Agent能力提及（模块围栏、变更边界核验）
2. ✅ 工作流框架（需求澄清→实现设计→DT设计→代码实现）
3. ❌ **缺少完整的围栏保护机制实现**

## 二、需要补充的功能

### 2.1 在 csi-requirement-design Skill 中增强围栏生成

**位置**：`skills/csi-requirement-design/SKILL.md`

**需要添加**：
- 明确的围栏分类（新增/修改/保护/范围外）
- 围栏配置的标准格式
- 模块依赖关系分析
- 禁止反向依赖/跨层调用/循环依赖的检查

### 2.2 创建 csi-fence-checking Skill

**功能**：
- 读取设计中的围栏配置
- 在实现前检查变更是否在围栏范围内
- 提供围栏突破流程
- 记录围栏突破原因

### 2.3 在 csi-code-implementation Skill 中集成围栏检查

**需要添加**：
- 实现前自动读取围栏配置
- 每个文件修改前的围栏校验
- 围栏突破的用户授权请求
- 围栏突破记录

### 2.4 在 csi-code-review Skill 中增强变更边界核验

**需要添加**：
- 对比实际diff与围栏配置
- 识别越界修改
- 提供围栏合规性检查结果

## 三、具体实施方案

### 方案一：轻量级补充（推荐）
1. 增强现有Skill的围栏意识
2. 在关键节点添加围栏检查提示
3. 保持现有工作流不变

### 方案二：完整集成
1. 复制AET的围栏模块到AR2Code
2. 创建专用的围栏检查Skill
3. 深度集成到工作流各阶段

## 四、关键文件需要修改

1. `configs/workflow-requirement-to-code-v1.yaml` - 集成围栏Skill
2. `skills/csi-requirement-design/SKILL.md` - 增强围栏生成
3. `skills/csi-code-implementation/SKILL.md` - 集成围栏检查
4. `skills/csi-code-review/SKILL.md` - 增强变更边界核验
5. （可选）新建 `skills/csi-fence-checking/SKILL.md`

## 五、围栏配置标准格式（参考AET）

```markdown
## 模块变更计划

### ✅ 允许修改
- [文件路径] - [修改原因]

### ❌ 禁止修改
- [文件路径] - [保护原因]

### 🔵 条件修改
- [文件路径] - [条件说明]
```
